# idea-score Tasks

This compact file is a representative stand-in for the fuller per-feature `tasks.md` artifact that Spec Kit normally manages.

1. Add or confirm the immutable `Idea` structure and scoring logic.
2. Enforce integer-only validation for `impact`, `effort`, and `strategic_fit`.
3. Cover valid scoring and out-of-range rejection in `tests/test_backlog.py`.
