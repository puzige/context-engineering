# idea-score Plan

This compact file is a representative stand-in for the fuller per-feature `plan.md` artifact that Spec Kit normally manages.

1. Define the `idea-score` behavior and validation rules in a compact specification.
2. Implement the immutable idea model and weighted scoring formula in `src/backlog.py`.
3. Verify valid scoring and invalid input handling with focused unit tests.
