# Task Summary: 01-01

- Implemented `Idea` frozen dataclass in `src/backlog.py`.
- Implemented `score_idea` checking that score fields are in [0, 5] range.
- Wrote two unit tests in `tests/test_backlog.py` to cover success scoring and out-of-range value exceptions.
- Executed unit tests and confirmed they pass.
