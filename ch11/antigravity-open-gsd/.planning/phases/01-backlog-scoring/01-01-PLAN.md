# Task Plan: 01-01-PLAN

<task type="auto">
  <name>Create Idea model and score_idea function</name>
  <files>src/backlog.py, tests/test_backlog.py</files>
  <action>
    - Declare frozen dataclass Idea with title (str), impact (int), effort (int), strategic_fit (int).
    - Implement score_idea(idea: Idea) -> int validating that impact, effort, and strategic_fit are integers between 0 and 5.
    - Write unit tests covering score calculation and validation checks.
  </action>
  <verify>python -m unittest discover -s tests -p "test_*.py" -v</verify>
  <done>All scoring logic verified and unit tests pass successfully.</done>
</task>
