# OpenCode workflow notes

- Refine the idea before writing code.
- Save the design to `docs/superpowers/specs/`.
- Save the plan to `docs/superpowers/plans/`.
- Keep the example minimal and verify with `python -m unittest discover -s tests -p "test_*.py" -v`.
