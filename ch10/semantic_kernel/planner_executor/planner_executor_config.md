# Planner and executor config

- Use this folder as the local example workspace.
- Keep the plan visible in `planner_executor_plan.md` while running the example.
- Keep the request and step list short so the execution order is easy to verify.
