# Tool use config

- Use this folder as the example workspace.
- Register one small tool named `lookup_example_code`.
- Keep the tool description aligned with `semantic-kernel-tool-use.md`.
- Keep the example minimal so the tool call is obvious when you run it.
