# n8n subworkflow boundary

This companion shows where a larger automation should be split into a reusable subworkflow.

Inspect `main.workflow.json` and `subworkflow.workflow.json` to see the handoff boundary.
